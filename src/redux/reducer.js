const initState = {
//     return object chứa danh sách sản phẩm
    products: []
}
export const rootReducer = (state= initState, action) => {
    switch (action.type){
        case "home/load-products":
        //         ???
        default: return state;
    }
}